# Production build settings. No secrets here.
#
# Without this the variable is undefined, USING_MOCK flips to true, and the
# dashboard answers its own requests from a store that lives in the browser —
# while the public site's submissions go to the store inside /api on the server.
# Two different stores, so a submitted title never appears in the library.
NEXT_PUBLIC_API_BASE_URL=/api
